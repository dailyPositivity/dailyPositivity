import React, { Component, useEffect, useState } from 'react';

const Counter = () => {
   const [count, setCount] = useState(0);
   useEffect(() => {
      // when rendered, setcount to 3000
      
   })
}
export default SlideshowBtns;
